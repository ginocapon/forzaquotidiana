name: Trigger Pages rebuild

# Obsoleto con source = GitHub Actions: l'API /pages/builds fallisce (404/403).
# Il deploy reale è solo deploy-pages.yml. Workflow disabilitato.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Skip
        run: echo "Usa il workflow Deploy GitHub Pages (push su main o workflow_dispatch)."
