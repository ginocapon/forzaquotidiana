# Payload usa SOLO PostgreSQL — NON l'URL https://...supabase.co
# NON servono NEXT_PUBLIC_SUPABASE_* (sono per altri progetti, non questa app)

# --- Opzione A: Supabase (cloud DB) ---
# Dashboard → Database → Connection string → URI
# Windows: usa SESSION POOLER (5432), NON Direct db.xxx.supabase.co (solo IPv6 → ENOTFOUND)
# postgresql://postgres.ieeriszlalrsbfsnarih:[PASSWORD]@aws-0-eu-central-1.pooler.supabase.com:5432/postgres

# --- Opzione B: Postgres locale con Docker (sviluppo rapido) ---
# docker compose up -d
DATABASE_URL=postgresql://training:training@localhost:55432/training

PAYLOAD_SECRET=forza-training-dev-secret-2026-min-32-chars
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Stripe (opzionale — module 11)
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_WEBHOOK_SECRET=whsec_...

# Dashboard Supabase: https://supabase.com/dashboard/project/ieeriszlalrsbfsnarih
