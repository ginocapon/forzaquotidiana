---
description: Upload foto Zepp/WhatsApp → WebP in pagina sessione come il 4 agosto
alwaysApply: true
---

# Foto sessione — subito in pagina (layout 4 agosto)

Quando Gino carica JPEG WhatsApp (GitHub *Add files via upload*, cartella `allenamenti/foto allenamento {data}/`, `allenamenti/allenamento …`, o **root del repo**), **non** chiudere il task con i JPEG nel repo e `<img>` che puntano a WebP inesistenti.

## Obbligo

1. **Identificare visivamente** ogni scatto (non fidarsi del nome WhatsApp né dell’ordine file). Se **non** è Zepp (meme, mappe, social) → **non** processare; usare cruscotto Excel/grafici e avvisare Gino.
2. **Radrizzare** con `sharp().rotate()` — molti JPEG WhatsApp arrivano ruotati 90°: controllare **sempre** il WebP prima di metterlo in pagina (riepilogo, zone FC, ecc.).
3. `node tools/processa-foto-upload.mjs` → WebP in `img/allenamenti/amazfit/YYYY-MM-DD-scheda-N-{slug}.webp` · **eliminare JPEG e cartella upload**.
4. Mettere le foto **dentro** `/allenamenti/sessioni/YYYY-MM-DD-scheda-N/` — layout fisso:
   - **Readiness:** `.amazfit-tsb-hero` → screenshot modulo TSB **solo, tutta larghezza** (mai in griglia ¼)
   - **Readiness gallery:** portrait sonno · HRV · HybridCharge sotto l’hero TSB
   - **Metabolico — ordine obbligatorio:**
     1. `.amazfit-riepilogo-hero` (o `.phone-shot--full`) → **resoconto Zepp solo, in primo piano**
     2. `.amazfit-fc-hero` → **grafico FC solo**
     3. `.amazfit-gallery` → zone · muscoli/radar · valutazione (portrait in griglia)
     4. `.amazfit-data` → card dati raggruppate (stessi numeri delle foto)
5. Screenshot Zepp = **documentali** (niente `data-ai` / Foto AI).
6. Commit + push se Gino ha chiede di pubblicare.

Riferimento visivo: `/allenamenti/sessioni/2026-08-04-scheda-2/` · dettaglio `SKILL-PERFORMANCE.md`.
